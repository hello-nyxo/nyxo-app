import React from 'react'
import styled from 'styled-components/native'

const MicroTasks = () => {
  return <Container />
}

export default React.memo(MicroTasks)

const Container = styled.View`
  margin-bottom: 30px;
`
